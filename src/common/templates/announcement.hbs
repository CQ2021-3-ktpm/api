<!DOCTYPE html>
<html>
<head>
  <style>
    body {
      font-family: 'Roboto', Arial, sans-serif;
      background-color: #f4f4f9;
      margin: 0;
      padding: 0;
    }
    .container {
      margin: 20px auto;
      padding: 30px;
      max-width: 600px;
      background: #ffffff;
      border-radius: 10px;
      box-shadow: 0 8px 15px rgba(0, 0, 0, 0.1);
    }
    .header {
      text-align: center;
      margin-bottom: 30px;
    }
    .header img {
      width: 150px;
      border-radius: 50%;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    }
    .content {
      color: #333333;
      font-size: 16px;
      line-height: 1.8;
      margin-bottom: 30px;
      text-align: center;
    }
    .content b {
      color: #007bff;
    }
    .cta {
      display: inline-block;
      padding: 15px 25px;
      background: linear-gradient(45deg, #007bff, #0056b3);
      color: white;
      font-size: 18px;
      font-weight: bold;
      text-decoration: none;
      border-radius: 50px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
      transition: all 0.3s ease-in-out;
    }
    .cta:hover {
      background: linear-gradient(45deg, #0056b3, #004494);
      box-shadow: 0 6px 12px rgba(0, 0, 0, 0.3);
      transform: translateY(-3px);
    }
    .footer {
      font-size: 14px;
      color: #777777;
      text-align: center;
      margin-top: 20px;
    }
    .footer p {
      margin: 5px 0;
    }
    .footer a {
      color: #007bff;
      text-decoration: none;
      font-weight: bold;
    }
    .footer a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
<div class="container">
  <!-- Header -->
  <div class="header">
    <img src="https://overlayforge.com/wp-content/uploads/2023/04/pepe_title-picture-1024x455.jpg" alt="Logo">
  </div>

  <!-- Content -->
  <div class="content">
    {{content}}
  </div>

  <!-- Footer -->
  <div class="footer">
    <p>Need help? <a href="mailto:support@ktpmapp.com">Contact Support</a></p>
  </div>
</div>
</body>
</html>
